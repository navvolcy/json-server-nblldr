{
  "name": "node-starter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "server": "json-server db.json --routes routes.json",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.6.3",
    "json-server": "0.17.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
